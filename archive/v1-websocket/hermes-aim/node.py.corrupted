# AIM 在线状态机制优化方案

> 问题：呱呱长连接在线但 Server 认为不在线

---

## 一、当前问题

| 问题 | 根因 |
|------|------|
| 短连接（aim_send.py）抢了长连接的 handler | 默认 channel=main，同 channel 踢旧连接 |
| Server 认为呱呱不在线 | 长连接 WS 的 is_active() 被标记 disconnecting |
| 消息存了离线但 agent 在线 | 离线消息只在 auth_ok 时推，连接恢复后不推 |

---

## 二、长短连接分离

### 短连接（aim_send.py）

```python
# 改为走 script channel，不影响 main 的 handler 状态
auth_payload["channel"] = "script"
auth_payload["handler"] = False
```

**改动后：**
- `aim_send.py` → channel=script, handler=false ✅
- `aim-agent.py` main → channel=main, handler=true ✅
- 短连接不再和长连接抢 handler
- `_deliver_single` 能正确找到 main 的 handler 连接

### 长连接（aim-agent.py）

保持当前设计：main + health 双通道，常驻监听。

---

## 三、消息通知机制（等待握手）

当 Server 有新消息要投递给 Agent 时：

```
方案 A：Server 推送通知（当前方式，但不依赖连接状态）
  
  消息到达 → _deliver_single(to)
    ├─ 有活跃 handler 连接 → 直接投递 ✅
    └─ 没有活跃连接 → 
        ├─ 存离线消息（当前已有）
        └─ 等 Agent 下次心跳/连接时推送（当前已有）

方案 B：新消息 → Server 发一个轻量通知
  1. 消息到达
  2. Server 发出一个小体积 notify 帧（几字节）
  3. Agent WS 收到 notify → 主动拉取消息
  4. 这不需要额外机制——async for 本来就在监听

方案 C：Agent 定时轮询（简单可靠）
  1. Agent 每 30 秒发一次心跳
  2. Server 在心跳响应里带 "你有 N 条未读"
  3. Agent 有未读时主动拉取
```

实际上**当前架构已经支持了**——Agent 监听 WS，Server 直接 push 消息。问题只是 `_deliver_single` 找不到活跃连接。

**根本修复：** 让 `_deliver_single` 不依赖 `is_active()` 的布尔判断，而是直接向所有 WS 连接推送。或者让 `is_active()` 更宽松——只要 WS 的 TCP 连接还在，就认为是 active：

```python
def is_active(self) -> bool:
    # 放宽条件：只要没有被标记 disconnecting，都算活跃
    # TCP 连接状态由 websockets 库自动管理
    return not self.disconnecting
```

当前 `is_active()` 就是这样实现的，问题不是这个方法而是 `_get_handler_conn` 里额外检查 `is_active()`。

**验证：** 看 pool_status 的 active 字段：

```
★main active=True term=1    ← 这个应该是 True
```

但 `online?` 返回的 active 对呱呱的 main 连接是啥？刚才查到的：

```
★main active=None age=31s   ← 这里是 None！
```

`active=None` 不是 False，是**没序列化**。看看 `online?` 的响应构造：

<｜｜DSML｜｜tool_calls>
<｜｜DSML｜｜invoke name="read_file">
<｜｜DSML｜｜parameter name="limit" string="false">20